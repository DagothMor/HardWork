# 1
## 1.1

Спецификацией гарантируется что придет жсон объект и у него обязательно будет конкретный ключ, а если нет?

```cs
public IActionResult GenerateDossier(JObject json)
{
	Guid contact = (json.Value<JToken>("contactId")).ToObject<Guid>();
	return Ok(_service.GenerateDossier(contact))
}
```
## 1.2
По идее заменяем значения в документе с чего то на чтото, но на вход подается массив байтов а не XLWorkBook
```cs
public byte[] ReplaceInXlsxTemplate(byte[] fileTemp...)
{
	
}
```

# 2

## 2.1
Мы используем древнюю лицензию старой версии платного решения, логично что мы очень близки к инцидентам, если не обновимся)
```cs
public byte[] TemplateHelpGetBody(JObject json)
{
	var result = TempHelper.GetDocBody(bytes)
}
```

# 3
Мы сами ходим в отдельные сервисы с приватными данными пользователя,чтобы узнать как у него дела с финансами, логичнее было бы, чтобы он приложил справку из госуслуг с сертификатом, так железно получаем то что нужно, не делая лишней логики, которая в будущем может быть изменена

# 4 

"У нас есть документация и статьи в конфлюенсе, мы от них отталкиваемся а до и после мы не используем)))"