# 1 IFileManager
Недавно был добавлена альтернативная реализация отправки файла, была подключена полноценная S3 и именно туда теперь отправляется файл методом Upload,аналогично и с Get.
# 2 IAnalystLog
Несколько реализаций, либо логи пишутся в бд, либо в графану.
___
И 1 и 2 пример настраивались во время билда всего бизнес процесса.
# 3 IUpdateService
Не добавлялась лишняя реализация, но косвенно пришлось ручками редактировать базовые параметры в запросах, а именно количество чанков на обработку.

# 4  IDbContext
Не добавлялась лишняя реализация, сидели на Postgres и дальше сидим, статей про простой переезд в большинстве базовых проектов с MS SQL на Postgres достаточно.

# 5 `ISettingsGetter<T>`
В будущем будет добавлятся альтернативное решение, а именно реализация, отталкивающаяся от значений в базе данных будет самой приоритетной, нежели в переменных окружения, на текущий момент это тот костыль, на реализацию которого выйдет меньше всего времени, чего конечно же нет у проектников :^).

Не знаю какой вывод писать, это удобно, да и вообще стройка через абстракции позволяет думать ввысь, нежели погружаясь в детали.

